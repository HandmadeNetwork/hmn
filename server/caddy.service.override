# When installing Caddy via their default Ubuntu instructions (see
# serversetup.sh), they will automatically create a systemd unit file. This
# file contains overrides to the contents of that automatic unit file and is
# expected to be installed at
# /etc/systemd/system/caddy.service.d/override.conf.

[Service]
ExecStopPost=/home/hmn/hmn/server/adminmailer.sh %n
